opts = [1, 2, 5, 10, 20, 50, 100, 200]
len  = length opts
sets = () -- where we store final results
checkSets = -- simple check to see if val is in sets
sumSet = -- alias to summing the set
coinSums current count =
  -- if sum current == 200 then
    -- if checkSets current
    -- then count
    -- else
      -- add to set
      -- count + 1
  -- else if sum current > 200
    -- then count
    -- else add an opt
